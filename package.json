{
  "name": "flow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.mjs",
    "dev": "node scripts/build-config.mjs && http-server . -p 8765 -o flow.html -c-1",
    "start": "npm run dev"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
